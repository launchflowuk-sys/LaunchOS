# Handoff — 11 Sep 2026

Read this first. The previous handoffs (`2026-09-10-handoff.md`, `2026-09-09-handoff.md`)
are still accurate about the site-build pipeline and older traps; this one supersedes them on
everything below.

**State:**
- Production is healthy and everything is pushed.
- The full test suite passes: 2,327 tests on the last full run, plus the sitegen tests added after it.
- No uncommitted work, and nothing half-built.
- Check `git log` for the exact HEAD; the last commits of this session are listed below.

---

## 1. Start here: what Shoji asked for and what is left

Shoji's list of 11 Sep, in his order. Two items are done and verified; three are next.

| # | Ask | State |
|---|---|---|
| 1 | Check the /start lead funnel emails the client and the admin | ✅ **Done, proven live** (see §3) |
| 2 | "Admin email" setting in the app instead of the hard-wired `OWNER_NOTIFY_EMAIL` | ❌ **Not started.** Build next; plan in §6 |
| 3 | WhatsApp alert alongside the email for new lead, brief, support request, urgent incident | ❌ **Not started.** Plan in §7; needs Twilio from Shoji to go live |
| 4 | Costing and profit per client and overall, monthly and yearly, incl. API costs of every automation | 📝 **Designed only**, blocked on Shoji's answers (§8) |
| 5 | Fix the website generator's JSON | ✅ **Done, verified against the real model** |

**Full spec for 2–4:** `docs/superpowers/specs/2026-09-11-admin-alerts-and-costing.md`. It is agreed
direction; the costing part is still open for discussion.

**Decisions Shoji has already made (do not re-ask):**
- WhatsApp goes through **Twilio**.
- Alert on all four groups: **new lead/enquiry, brief submitted, new support request, urgent incidents**.
- Shared running costs split **by measured usage**; the unmetered remainder is split evenly across active clients.
- Build order: admin email → alerts and WhatsApp → costing.

---

## 2. What shipped on 11 Sep

| Commit | What |
|---|---|
| `d8591a0` | **Per-client service switches.** Ads / Blog / Social / GBP are off until a person switches them on (client → **Services** tab). Website is always on |
| `633ad8d` | Review fixes: AI image generation gated by the service; advisory lock on the first toggle |
| `21fb467` | `infra/server/launchos-image-cleanup.sh`: LaunchOS-only image cleanup after every deploy |
| `e4a1f95` | **Brief writer fixed.** It never sent its schema; now uses strict `json_schema`, gives up after 3 attempts, bell once |
| `9343ed2` | Spec: admin alerts, WhatsApp, costing |
| `49f18b3` | **Website generator** now uses strict `json_schema` too (it had the same `json_object` flaw) |
| this commit | This handoff |

### Service switches: how they work (read before touching any job that spends money)
- **Data:**
  - Table `client_services` (`packages/db/src/schema/client-services.ts`); no row means off.
  - Logic in `packages/core/src/clients/services.ts`.
- **Shoji's decisions:**
  - All existing clients start OFF. That is live now, so nothing runs for any client until switched on.
  - Switching off **holds** work; nothing is cancelled.
  - Switches are per client, not per website.
- **Gates** (every one has tests):
  - `planContentMonth`, `clientsOwedContent`
  - `handleContentDraft` (writes a skipped `agent_runs` row)
  - `content_list_slots` (`held` slots)
  - `claimDueContent` (holds rather than claims)
  - `fanOutPublishedPost`
  - `listAdAccounts({ managedOnly: true })`, used by ingest, campaign ingest and `ads_list_accounts`
  - `buildSentinelJobs`
  - `generateRecurringTasks`
  - `renderContentImage`
  - Manual "Draft with AI" and "Draft from website" buttons
- **Rule for new code:** any new job that spends money on a client must check `activeServicesForClient`.
  Test fixtures must call `activateClientServices` from `@launchos/db/test`, or the gate makes the
  test assert on nothing.
- **Merge:** `client_services` is in `MOVE_SPECS` and `merge-words.ts`.
- **Not verified in a browser:** the Services tab. No agent can sign in, so Shoji must look.

---

## 3. Verified live on 11 Sep (facts, not assumptions)

### Email
- **Outbound:**
  - Postmark SMTP (`smtp.postmarkapp.com:587`), From `support@launchflow.co.uk`. Delivered.
  - The 2 "SMTP API error" bounces on 10 Sep were before that sender was confirmed. They are historical.
- **Inbound:**
  - **Live on the support domain** since today. Postmark server **InboundDomain =
    `support.launchflow.co.uk`**, set via API.
  - MX points to `inbound.postmarkapp.com`.
  - A direct MX send became a case under "Unmatched inbound".
  - Webhook auth: a wrong secret gets 401.
- **`INBOUND_EMAIL_ENABLED=1`** is set on web and worker in Coolify (Coolify auto-created the preview copies).
  Verified in the running containers. Support emails now invite replies by email.
- **Test messages in production:** 5 "LaunchOS inbound smoke test … safe to close" cases under
  "Unmatched inbound", plus a Support Triage task. **Shoji to close them.**

### Lead funnel (launchflow.co.uk/start, 8 steps)
- A live test brief was submitted: **LF-YFPN-9Y52**, business "LaunchOS Smoke Test Ltd", email
  `smoke-test@support.launchflow.co.uk`.
- Owner email "Website brief: …" was **delivered** to `shujaat@launchflow.co.uk`.
- Client email "We have your brief" was **delivered**.
- The lead was created (source `brief-funnel`), and `lead.captured` and `brief.submitted` bells fired.
- **Console errors are not bugs:**
  - The 404 is `GET /api/brief-funnel/session` when no draft exists.
  - The two 422s were step-7 saves before a timing box was ticked.
- **Timing is a checkbox group:** clicking both the box and its label unticks it. That confused the test and is not a bug.
- **Test lead "LaunchOS Smoke Test"** can be deleted.

### Brief writer
- **Before:** it never worked. `response_format: json_object` was sent with no schema, so every reply failed Zod.
  Every failure rang the bell, 109 times in 24h. Separately, OpenAI credit ran out on 11 Sep
  (Shoji topped it up).
- **After the fix:** LF-RMGF-3VCB and LF-YFPN-9Y52 both have version 2 (`brief-writer-openai`,
  `gpt-6-astra`). Zero `brief.write_failed` bells since the deploy.
- **Pattern to reuse** for any strict structured output: `packages/integrations/src/brief-writer/openai-schema.ts`.
  - Zod → `z.toJSONSchema` → closed objects, all properties required, optional fields as nullable,
    `const` as enum, length limits stripped.
  - `dropNulls` runs before Zod parses the reply.
- **Give-up bookkeeping:** in `brief_submissions.metadata` as `briefWriteAttempts`,
  `briefWriteLastError`, `briefWriteLastAttemptAt` and `briefWriteGaveUpAt`.
  - **There is no "run again" button.** Retrying a given-up brief means clearing `briefWriteGaveUpAt`.
  - A small button on the lead would be a sensible follow-up.

### Website generator
- **Now:** strict `json_schema` (`GENERATED_SITE_SCHEMA` in `packages/integrations/src/sitegen/openai.ts`).
- **Real model, sample brief:** 3 pages plus CSS, valid, in 48s.
- **No `site_builds` rows exist in production yet.** The pipeline has never run live.

### Disk and cleanup
- **Each push** adds about 5.4 GB of images (web 2.76 GB + worker 2.66 GB) plus build cache.
- **`/usr/local/bin/launchos-image-cleanup`:**
  - Runs from root's crontab every 5 minutes.
  - Only removes images whose repository is **exactly** a LaunchOS app uuid.
  - Keeps the newest 2 per app, never removes an image any container uses, never forces, never prunes.
  - Log: `/var/log/launchos-image-cleanup.log`.
- **Verified:** it removed exactly the two old LaunchOS images; every other app's image was untouched.
- **Build cache** is shared by every app and cannot be attributed. It is only cleared by the weekly
  `/usr/local/bin/docker-reclaim` (Sun 04:00, all apps, images + build cache, never volumes).
  **Shoji said the new cleanup must never touch other deployments.** Do not add build-cache
  pruning to it.
- **Disk:** 37% (27 GB / 75 GB) at 04:52 UTC.

---

## 4. Owed by Shoji (tell him, do not do it for him)

1. **Switch services on** for every client who should get ads or posts (client → Services).
   Everything is off since deploy. AMO Rendering's ad figures will not update until ads is on.
2. **Google Ads is still broken:** `deleted_client`. He enabled the API; he still needs a new OAuth client
   (Web application, redirect `http://localhost:8787/callback`), then
   `node scripts/google-ads-refresh-token.mjs <id> <secret>`, then paste 3 values into Coolify on
   **both** web and worker. Alternative without localhost: OAuth Playground. Consent screen must be
   **In production** or the token dies in 7 days.
3. Close the 5 smoke-test cases and delete the test lead (§3).
4. **Twilio**, for WhatsApp:
   - an account;
   - a **second** number as sender (never his WhatsApp Business number, see the 7 Sep pricing spec);
   - one approved utility template;
   - the number(s) alerts go to.
5. **Costing answers:**
   - (a) every subscription with price, billing period and business (LaunchFlow / Cabio / shared);
   - (b) whether to reconcile against real OpenAI/Anthropic bills (needs admin usage keys);
   - (c) VAT: ex-VAT (recommended) or inc-VAT.
6. **Carried from 10 Sep, not checked today:**
   - merge Ahmed Mohebi into Gateway Taxis;
   - publish the 39 help guides;
   - AMO billing £99 → £200.
7. Look at the client **Services** tab in the browser (not browser-verified).

---

## 5. Traps that cost time today

- **SSH:** `ssh -i ~/.ssh/launchos_hetzner root@88.198.146.183`. Do not cycle keys (fail2ban).
  - DB: `docker exec -i krkkrmokqncgb8y44vcinygv psql -U launchos -d launchos`.
  - App uuids: web `tm9ihp9bjjjeew5g8nzuqqsg`, worker `n7sb6hq1fnuvfufmvstwrotf`.
- **Coolify API:** `COOLIFY_API_URL` and `COOLIFY_API_TOKEN` are in the repo `.env`.
  - Deploy truth: `GET /api/v1/deployments/applications/{uuid}?take=1` gives `commit` + `status`.
  - Env: `GET /api/v1/applications/{uuid}/envs`. Never print values; mask secrets.
- **Postmark:**
  - Server token = the `SMTP_USER` env value. The API gives outbound/inbound history, bounces and `/server`.
  - Its lists lag about a minute. DB ingest of an inbound mail can take about 90s. Wait before calling a test lost.
- **`messages` table** only holds support-thread mail. Owner, brief and invoice emails leave no row; use Postmark's API.
- **Running platform code inside the worker container:** packages resolve to TypeScript `src`, so
  `import("@launchos/channels")` fails under plain node. Use nodemailer via
  `createRequire("/app/packages/channels/package.json")`.
- **Scratch scripts outside the repo** cannot resolve `zod`. Either import repo files by `file:///` URL
  (they resolve their own deps) or run tests inside the package.
- **Notifications:**
  - `notifyOwner` **never sends email**, only a bell plus push for urgent kinds (`packages/core/src/push/urgent.ts`).
  - A **support request** (`create-ticket.ts:134-151`, `ingest-inbound-email.ts:143,193`) and **site down**
    (`open-incident.ts:19`) notify the owner **not at all**.
  - `worker.down` is only checked when someone loads an admin page (`apps/web/src/app/(admin)/layout.tsx:33`).
- **OpenAI** answers 429 both for rate limits and for no credit (`insufficient_quota`). Read the body.
- **Do not run `pnpm build` while `pnpm dev` runs** (CLAUDE.md). The full test suite needs `pnpm db:up`.

---

## 6. Next build: Admin email setting (small, start here)

Follow the existing organisation-settings pattern exactly: `packages/core/src/assignment/rules.ts`
(tolerant reader, merge into `organisations.metadata`, audited).

1. **Core:** `packages/core/src/notifications/settings.ts`.
   - `NotificationSettings` (Zod): `adminEmails: string[]` (1–5 emails, first is main),
     `whatsappNumbers: string[]` (E.164, max 5), `whatsappEnabled: boolean`.
   - Metadata key `notifications`.
   - Functions: `notificationSettingsFrom(metadata)`, `getNotificationSettings(db, org)`, and
     `setNotificationSettings(db, org, { settings, actorId })`. The setter merges like
     `setAssignmentRules` (rules.ts:67-93) and audits as `organisation.notifications_updated`.
   - `adminEmailRecipients(db, org, env)` returns saved `adminEmails`, else `[env.OWNER_NOTIFY_EMAIL]`,
     else `[]`. The fallback keeps things working on deploy day.
   - Export from `packages/core/src/index.ts`. Write the Vitest tests first.
2. **Replace every reader of `OWNER_NOTIFY_EMAIL`:**
   - `packages/core/src/brief-funnel/submitted-emails.ts:56`: send to every recipient. It already has `db` and `organisationId`.
   - `apps/worker/src/jobs/ops-brief.ts:111`: `deps.db` is available.
   - `apps/web/src/app/(admin)/settings/email/actions.ts:32` and `page.tsx:72,183`.
   - The worker's `env.ts:34` stays as the fallback declaration.
3. **Web:** Settings → **Notifications**.
   - Files: `apps/web/src/app/(admin)/settings/notifications/page.tsx` and `actions.ts`.
   - Actions: `requirePermission("settings")`, Zod, core setter, `revalidatePath`, return `ActionResult`.
   - Nav entry in `apps/web/src/lib/nav.ts` beside "Settings" (Organisation group, around line 169),
     `permission: "settings"`.
   - Page pattern: `settings/email/page.tsx` (PageHeader, Section, `ActionForm`).
   - Include "Send test email" to the saved addresses.
   - White/light design, `launchflow-design` skill.
   - Remember: a `"use client"` file must never value-import `@launchos/core`.

## 7. Then: alerts by email and WhatsApp

Full detail is in the spec, Part 2. Key points:
- **Core `alertAdmins(db, org, { group, kind, title, body, link, dedupeKey })`:** the bell as now, then
  enqueue `alert.deliver`. The worker sends email to `adminEmailRecipients` and WhatsApp to
  `whatsappNumbers` when enabled. Dedupe per key; max 20 WhatsApp alerts an hour per organisation,
  then an "and N more" summary.
- **Raise it from:**
  - `brief-funnel/capture.ts:128` (`lead.captured`)
  - `leads/leads.ts:96`
  - `funnels/sessions.ts:301`
  - `brief-funnel/submit.ts:175`
  - **new:** `support/create-ticket.ts:134-151` and `ingest-inbound-email.ts:143,193`
  - **new:** `monitoring/open-incident.ts:19`
  - `billing/webhook-sync.ts:265` (`payment.failed`, inside a transaction, so enqueue after commit)
  - `heartbeat/alerts.ts:67`
- **WhatsApp adapter** (`packages/channels/src/sms/`):
  - Extend `SendSmsInput` (`types.ts:22-38`) with optional `contentSid` and `contentVariables`.
  - `twilio.ts:83-113` sends `ContentSid` and `ContentVariables` (JSON) and drops `Body` when a template is used.
  - A `whatsapp:` prefix on both numbers.
  - New env: `TWILIO_WHATSAPP_FROM`, `TWILIO_WHATSAPP_ALERT_TEMPLATE_SID`.
  - SMS fallback when `TWILIO_SMS_FROM` is set.
  - Mock-first, per rule 4.
  - Also fix: the worker's `env.ts` does not declare `TWILIO_*`, and `.env.example` lacks `TWILIO_SMS_FROM`.
- **Settings → Notifications:** add WhatsApp numbers, the switch and "Send test WhatsApp".
- **Open question for Shoji:** an outside worker-down check. A cron on the Hetzner box hits a web
  endpoint that alerts when the worker heartbeat is stale.

## 8. Costing (after 6 and 7, and only with Shoji's answers)

See spec Part 3. In short, three ledgers:
- a cost register (generalise `supplier_costs`, which is Hostinger-only today, with a `business` column);
- a `usage_events` ledger priced from an editable rate card with effective dates;
- revenue, which already exists.

**Meter first** (the variable costs):
- agent runs (`agent_runs.tokens_in/out` already exist; add the cost);
- the OpenAI brief writer and site generator (read `usage` from the response);
- images (`image-budget.ts` already estimates pence);
- ScreenshotOne captures, Postmark sends, Twilio messages.

**Screens:** Finance → Profit, the client Profit tab, and Settings → Costs.
**Reporting:** in GBP, with the daily FX rate stored.
**Reconciliation:** against provider usage APIs where keys exist.

---

## 9. Still open from before (unchanged today)

- The site-build pipeline (10 Sep handoff) is built but has never run live. SFTP credentials are in Coolify.
- Coolify API is on `http://`, so the token crosses the internet in cleartext. It needs HTTPS and rotation.
- `heartbeat.test.ts` was flaky before. All of today's full runs passed.
