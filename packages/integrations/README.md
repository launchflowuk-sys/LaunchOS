# @launchos/integrations
External provider clients, mock-first: `coolify` (hosting resources, deploy status), `cloudflare` (DNS), `google-ads`, `meta-ads`, `uptime` (HTTP probe). Each exports an interface, a `Mock*` implementation and a real implementation selected by env.
